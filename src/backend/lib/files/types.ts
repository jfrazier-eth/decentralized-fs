export interface PGFile {
  id: string;
  user_id: string;
  transaction_id: string;
  name: string;
  created_at: Date;
}

export interface File {
  id: string;
  userId: string;
  transactionId: string;
  name: string;
  createdAt: Date;
}
